<!DOCTYPE html>
<html>
  <head>
    <title>Bookmark Manager</title>
  </head>

  <body>
    <% if current_user %>
      Welcome, <%= current_user.email %>
    <% end %>

    <div class="validation_error">
      <%= flash[:error] %>
    </div>
    
    <%= yield %>
  </body>
</html>
